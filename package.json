{
  "name": "bible-plan",
  "version": "1.0.0",
  "private": true,
  "description": "Calendar-based Bible reading tracker with Supabase accounts.",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "node scripts/build.mjs",
    "dev": "vercel dev",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
